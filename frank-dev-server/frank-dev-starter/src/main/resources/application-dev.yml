spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://117.72.203.229:5432/frankdev?reWriteBatchedInserts=true
    username: frankdev
    password: frankdev
    druid:
      initialSize: 5                                # 初始连接数
      minIdle: 10                                   # 最小空闲连接数
      maxActive: 20                                 # 最大连接数
      maxWait: 60000                                # 获取连接最大等待时间（毫秒）
      connectTimeout: 30000                         # 建立连接超时时间（毫秒）
      socketTimeout: 60000                          # 读取超时时间（毫秒）
      timeBetweenEvictionRunsMillis: 60000          # 检测空闲连接间隔（毫秒）
      minEvictableIdleTimeMillis: 300000            # 连接最小空闲时间（毫秒）
      maxEvictableIdleTimeMillis: 900000            # 连接最大空闲时间（毫秒）
      webStatFilter:
        enabled: false                               # 开启 Web 监控过滤器，监控接口请求时间
      statViewServlet:
        enabled: false                               # 启用 Druid 控制台
        allow:                                      # 白名单，空表示允许所有
        url-pattern: /druid/*                       # 控制台访问路径
        login-username: admin                    # 控制台用户名
        login-password: admin                      # 控制台密码
      filter:
        stat:
          enabled: false                             # 启用 SQL 监控
          log-slow-sql: false                        # 记录慢 SQL
          slow-sql-millis: 1000                     # 慢 SQL 阈值（毫秒）
          merge-sql: true                           # 合并 SQL 统计
        wall:
          config:
            multi-statement-allow: true             # 允许多条 SQL 执行
  data:
    redis:
      host: 117.72.203.229
      port: 6379
      password: redis
      database: 0
      lettuce:
        pool:
          max-active: 10
          max-idle: 10
          min-idle: 0
          max-wait: -1ms

# logging configuration.
logging:
  sql:
    enabled: true
  config: classpath:log4j2.xml      # 日志配置文件路径
  level:
    com.frank: INFO
    org.springframework.web: INFO
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"