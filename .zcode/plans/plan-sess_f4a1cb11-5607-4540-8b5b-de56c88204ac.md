## 实施目标
继续完成 Elasticsearch 系列剩余 09 篇文章（02–10），不是只补大纲。所有文章放在 `source/_posts/articles/Elasticsearch/`，每篇采用零填充文件名与同名 post asset folder，延续第 01 篇的中文教程风格、ES 8.x 语法、Java 开发者视角和 `blog_articles` 贯穿示例。

## 文章批次与内容

### 批次 A：可运行基础（02–04）

1. `02-elasticsearch-docker-kibana-crud.md`
   - Docker Compose 启动 Elasticsearch + Kibana 8.x
   - 单节点开发环境与安全认证说明
   - 健康检查、密码、端口和持久化目录
   - Kibana Dev Tools 与 curl 的使用差异
   - Index、Document 的完整 CRUD
   - Bulk 基础、refresh 与近实时可见性
   - 常见启动/连接/认证问题
   - 配图：Docker 组件拓扑、一次 CRUD 调试链路

2. `03-elasticsearch-mapping-analysis-chinese.md`
   - 显式 Mapping 与动态 Mapping
   - `text`、`keyword`、数值、日期、`object`、`nested`、地理字段
   - Multi-fields 与字段用途拆分
   - Analyzer、Tokenizer、Token Filter
   - `_analyze` API 验证索引期和查询期分析
   - IK 分词器安装、ES 主版本兼容和 `ik_smart`/`ik_max_word`
   - 自定义词典、同义词和中文搜索常见问题
   - Mapping 不能直接修改时的重建索引方案
   - 配图：Analysis 管道、中文字段设计对比

3. `04-elasticsearch-query-dsl.md`
   - Request Body Search 基础结构
   - `match`、`multi_match`、`match_phrase`
   - `term`、`terms`、`range`、`exists`
   - `bool` 的 `must`、`filter`、`should`、`must_not`
   - 相关性、`boost`、`function_score` 基础
   - 排序、`_source` 过滤和高亮
   - `explain` / Profile 的诊断用途
   - 配图：bool 查询组合结构、相关性与高亮结果流程

### 批次 B：应用检索（05–07）

4. `05-elasticsearch-aggregations-pagination.md`
   - `terms`、`range`、`date_histogram` 桶聚合
   - `avg`、`sum`、`min`、`max`、`cardinality` 指标聚合
   - 查询过滤与聚合过滤的区别
   - 商品/文章搜索筛选面板响应设计
   - `from + size` 的浅分页边界
   - `search_after`、Point in Time（PIT）和 Scroll 的适用场景
   - 配图：聚合结果树、分页方案对比

5. `06-elasticsearch-java-client-springboot.md`
   - Elasticsearch Java API Client 与旧 Transport Client 的演进
   - Spring Boot 连接、HTTPS、认证和超时配置
   - Typed Query / Request / Response 的构建
   - Service 层封装全文搜索、过滤、排序、聚合和高亮
   - 统一分页响应与异常处理
   - Repository 与原生 Java API Client 的选择边界
   - 集成测试的测试容器/独立 ES 边界说明
   - 配图：Spring Boot 到 ES 的请求架构、Java 查询对象映射

6. `07-elasticsearch-mysql-sync.md`
   - MySQL 作为事务事实来源、ES 作为搜索读模型
   - 应用双写、消息队列、Binlog CDC、定时全量/增量的比较
   - `_id` 设计、幂等写入、删除传播和重试
   - 消息积压、死信、同步延迟和数据校验
   - 全量初始化、增量补偿和按索引重建
   - Alias 切换与零停机读流量迁移
   - 版本冲突和最终一致性的业务处理
   - 配图：MySQL 到 ES 的同步链路、故障补偿闭环

### 批次 C：生产与综合实战（08–10）

7. `08-elasticsearch-bulk-reindex-performance.md`
   - Bulk API 批量导入和批次大小
   - refresh、replica、并发和背压
   - 导入期间的性能权衡与恢复设置
   - Reindex API、Alias 和零停机 Mapping 迁移
   - 分片数量、分片大小、Routing 的规划原则
   - 查询 Profile、慢日志、缓存和 `_source` 控制
   - 深分页与高成本 Wildcard/Script 查询避坑
   - 配图：Bulk 写入背压流程、Alias 零停机重建流程、优化排查路径

8. `09-elasticsearch-cluster-ha-security.md`
   - Master/Data/Ingest/Coordinating 节点角色
   - 主分片、副本分片分配与集群健康状态
   - 未分配分片、磁盘水位、JVM/GC 和节点故障排查
   - 分片恢复、故障转移和滚动维护
   - Snapshot Repository、备份与恢复演练
   - TLS、账号、角色权限和最小权限原则
   - 监控指标与生产上线检查清单
   - 配图：集群节点与分片分布、故障恢复路径、备份恢复流程

9. `10-elasticsearch-comprehensive-practice.md`
   - 以文章/商品搜索为例完成端到端实现
   - MySQL 数据模型与搜索 Index Mapping
   - 初始化同步、增量同步和失败补偿
   - Spring Boot 搜索接口、全文查询、筛选、聚合、高亮
   - `search_after`/PIT 分页和业务排序
   - 索引重建、Alias 切换、测试数据与验收指标
   - 生产检查清单、常见故障和系列总复习
   - 配图：完整系统架构、搜索请求时序、上线验收闭环

## 文章之间的导航

- 更新第 01 篇文末的“下一篇预告”，改为 `{% post_link articles/Elasticsearch/02-elasticsearch-docker-kibana-crud '...' %}`。
- 每篇 02–10 添加上一篇/下一篇的 `{% post_link %}`，并在文末提供完整或相邻章节目录。
- 更新 `source/about/index.md`，将 Elasticsearch 系列从“1 篇”扩展为 10 篇，并添加 02–10 的站内链接。
- 不使用硬编码日期 URL，避免后续调整 front matter 日期导致链接失效。

## 日期与文件约定

沿用已有系列的零填充命名和连续发布时间约定：第 02–04 篇作为第一批使用 20 分钟递进时间，第 05–07 篇和第 08–10 篇分别使用后续日期的 20 分钟递进时间。具体采用：

- 02–04：2026-08-24 10:20、10:40、11:00
- 05–07：2026-08-25 10:00、10:20、10:40
- 08–10：2026-08-26 10:00、10:20、10:40

## 配图与质量控制

- 每篇生成 2–3 张自包含 SVG；简单命令型文章控制在 2 张，架构/综合文章使用 3 张。
- 统一保存到对应文章同名目录，Markdown 只写裸相对文件名。
- 严格遵循 `research-svg` 的 Nature/NPG 配色、白底、细边框、中文字体栈、最小 11px 文本和底部图注规范。
- 不制作虚构性能数据图；需要比较时使用结构性对比图或明确标注“示意”。

## 导航与栏目文件

- `themes/oranges/layout/archive.ejs` 已有 Elasticsearch 映射，不重复修改。
- `README.md` 已有 Elasticsearch 内容地图，不重复修改。
- 仅在 `source/about/index.md` 扩展系列目录。

## 验证流程

1. 检查每篇文章的 Front Matter、编号、标题、`post_link` 和 SVG 引用是否一一对应。
2. 用 Python `xml.etree.ElementTree` 解析全部新增 SVG，检查 XML 合法、无外部依赖、字体尺寸不低于 11px。
3. 执行 `pnpm build`，确认 02–10 全部文章页、资源图、标签页、归档页和关于页正常生成。
4. 用 `git diff --check` 检查空白错误，并用 `git status --short` 确认只报告本次 Elasticsearch 扩展及用户已有工作区内容；不提交、不推送、不覆盖 RuoYi 草稿。