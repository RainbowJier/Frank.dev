## 全系列整改方案

目标：重写 `D:\Projects\Frank.dev\source\_posts\articles\Mybatis\` 下的 5 篇文章，使读者可以直接在最新项目 `D:\Projects\mini-mybatis-lab` 中逐步创建文件、敲代码和运行测试。

### 统一整改原则

- 不再使用 `lab`、`mini-mybatis-03` 等虚构工程。
- 不再使用“把所有代码拼到一个嵌套类外壳”的写法。
- 所有示例都使用真实根包 `com.frank.mybatis`。
- 框架代码放 `src/main/java/com/frank/mybatis/`；测试实体、Mapper、H2 测试支持和 Demo 放 `src/test/java/com/frank/mybatis/`。
- 每个实现步骤都明确：文件路径、package、前置依赖、职责、核心代码、完成后的测试命令。
- 文章使用当前项目已有的 `MiniMybatisApplication` 和 `schema.sql`，不重复提供另一份 pom 或启动类。
- 保持章节增量关系：01 固定注解 SQL → 02 XML/statement id → 03 动态 SQL/参数 → 04 缓存/插件/嵌套映射 → 05 类型/事务生态。

### 文章 01：固定注解 SQL与核心执行链

重写 `01-mybatis-mini-framework.md`：

- 修复 4 张 SVG 引用为同名资源目录下的真实相对路径。
- 以当前项目现有文件为起点：`pom.xml`、`MiniMybatisApplication.java`、`schema.sql`。
- 按 checkpoint 逐个创建真实文件：
  - `annotations/Select.java`、`Insert.java`、`Update.java`、`Delete.java`、`Param.java`
  - `mapping/SqlCommandType.java`、`PreparedSql.java`、`MappedStatement.java`、`SqlTemplateParser.java`
  - `executor/ParameterHandler.java`、`ResultSetHandler.java`、`Executor.java`、`SimpleExecutor.java`
  - `transaction/Transaction.java`、`JdbcTransaction.java`
  - `session/SqlSession.java`、`DefaultSqlSession.java`、`SqlSessionFactory.java`、`DefaultSqlSessionFactory.java`
  - `binding/MapperProxy.java`、`MapperProxyFactory.java`、`MapperRegistry.java`
  - `session/Configuration.java`、`builder/MapperAnnotationBuilder.java`
  - `src/test/java/.../fixture/User.java`、`support/H2DatabaseSupport.java`、`chapter01/UserMapper.java`、`chapter01/MiniMybatisChapter01Test.java`
- 每个代码块使用可直接复制的独立文件格式，解释 import、package 和依赖顺序。
- 明确第 01 章限制：固定注解 SQL、简单 `#{name}`、POJO/List 映射；不实现 XML、`${}`、动态 SQL、缓存和插件。
- 提供逐阶段验收测试和 `mvn clean test` 命令，避免使用未定义的 `Demo` 或伪方法。

### 文章 02：XML、注解与 statement id

重写 `02-mybatis-xml-and-statement-id.md`：

- 删除旧的重复段落和虚构包名。
- 以第 01 篇完成状态为前置，明确哪些文件是修改、哪些是新增。
- 按真实路径新增：
  - `builder/XMLConfigBuilder.java`
  - `builder/XMLMapperBuilder.java`
  - `builder/MapperAnnotationBuilder.java`
  - `mapping/MappedStatement.java`
  - `session/Configuration.java`
  - `binding/MapperMethod.java`（如需要独立封装方法到 ID 的转换）
  - `src/test/resources/mappers/UserMapper.xml`
  - `src/test/java/.../chapter02/UserMapper.java` 与 XML 集成测试
- 完整说明 namespace + statement id、XML/注解统一注册、classpath 资源加载、重复 ID/namespace/重载校验和 XML 外部实体防护。
- 给出从 `Method` key 迁移到 `String statementId` 的修改顺序，避免读者同时维护两种索引。
- 运行命令使用项目现有 Maven 配置，不嵌入独立 pom。

### 文章 03：动态 SQL与丰富参数

重写 `03-mybatis-dynamic-sql-and-parameters.md`：

- 删除旧的嵌套类工程和重复内容。
- 按真实包落文件：
  - `scripting/SqlNode.java`、`IfSqlNode.java`、`WhereSqlNode.java`、`TrimSqlNode.java`、`ForEachSqlNode.java`、`DynamicContext.java`
  - `mapping/SqlSource.java`、`StaticSqlSource.java`、`DynamicSqlSource.java`、`BoundSql.java`、`ParameterMapping.java`
  - `type/TypeHandler.java`、`TypeHandlerRegistry.java` 及基础处理器
  - `executor/ParameterHandler.java` 的增量改造
  - `src/test/resources/mappers/DynamicUserMapper.xml`
  - `chapter03` 参数与动态 SQL 测试
- 解释 Bean、Map、Collection、数组和标量参数对象；区分缺失值与 null。
- `if`、`where`、`trim`、`foreach` 每次只增加一个可验证能力。
- `${}` 只允许程序控制的白名单标识符，严禁把用户输入直接拼接；`#{}` 始终走预编译参数。
- 不直接依赖 OGNL 黑盒；先实现受限表达式子集，再对照真实 MyBatis。

### 文章 04：缓存、插件与嵌套映射

重写 `04-mybatis-cache-plugin-and-nested-mapping.md`：

- 删除旧文中的重复模板段落及错误的下一篇预告。
- 先说明当前 `schema.sql` 只有 `t_user`；嵌套映射实验需在 `src/test/resources/schema-ch04.sql` 增加测试专用 user/order/order_item 表，不能假定主表结构已有订单表。
- 按真实包新增：
  - `cache/Cache.java`、`PerpetualCache.java`、`CacheKey.java`、`TransactionalCache.java`
  - `plugin/Interceptor.java`、`Invocation.java`、`Plugin.java`、`Signature.java`
  - `mapping/ResultMap.java`、`ResultMapping.java`
  - `executor/ResultSetHandler.java` 增量改造
  - `chapter04` 缓存、插件、JOIN、association/collection 测试夹具
- 分别讲清一级缓存 Session 边界、二级缓存提交/回滚边界、更新失效、插件链签名校验、父子对象去重和 LEFT JOIN 空子项。
- 所有代码示例标明新增文件或修改文件，不混淆跨章节版本。

### 文章 05：类型、事务与生态

重写 `05-mybatis-types-transactions-and-ecosystem.md`：

- 删除旧文重复生成内容和没有依赖说明的 Spring 片段。
- 按真实包新增/修改：
  - `type/JdbcType.java`、`EnumTypeHandler.java`、`LocalDateTimeTypeHandler.java`、`TypeHandlerRegistry.java`
  - `executor/BatchExecutor.java`
  - `transaction/TransactionFactory.java`、`JdbcTransactionFactory.java`
  - `session/Configuration.java`、`DefaultSqlSessionFactory.java` 的增量改造
  - 可选 `spring/` 适配模块，并明确需要新增 Spring 依赖后才能编译
- 逐步实现 null、枚举、日期时间、JSON、主键回填和批处理。
- 清楚区分 JDBC 本地事务、连接池归还和 Spring 外部事务管理，不能把本地 Session.close 行为直接套到 Spring 环境。
- 给出每一阶段对应的测试和依赖变更，不让读者复制无法编译的 Spring API。

### 系列导航与资源

- 保留现有 Mybatis 专栏和 `source/about/index.md` 的 5 篇链接，但重写文章 front matter 描述，使标题、日期和章节职责一致。
- 第 01 篇图片改为正确引用：`01-mybatis-mini-framework/<svg-name>.svg`；第 02–05 篇如不新增图片，不创建空资源目录。
- 如文章需要新图，严格放对应文章同名资源目录，使用现有科研风 SVG 规范。

### 校验

1. 对每篇文章检查 front matter、真实项目路径、package 名、代码块中的文件路径和跨篇引用。
2. 修复第 01 篇 4 个 SVG 相对引用并确认本地文件存在。
3. 检查 02–05 篇不再出现 `lab`、`DynamicSql.java`、独立 pom 或重复模板段落。
4. 执行博客仓库 `pnpm build`，确认 5 篇文章和图片均正常生成。
5. 只修改 Mybatis 5 篇文章及必要的资源引用，不修改 `mini-mybatis-lab` 项目代码。