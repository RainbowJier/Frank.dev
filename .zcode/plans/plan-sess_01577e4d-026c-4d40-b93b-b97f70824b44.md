将现有文章作为《手写 MyBatis 01》，保留核心 JDBC、动态代理、SqlSession、Executor 和结果映射实现；新增四篇独立增量文章并统一放在 `source/_posts/articles/Mybatis/`：

1. `01-mybatis-mini-framework.md`：现有核心执行链，标题改为系列第 01 篇。
2. `02-mybatis-xml-and-statement-id.md`：引入 XML Mapper、namespace、statement id、XML 与注解统一注册、重载冲突和资源加载。
3. `03-mybatis-dynamic-sql-and-parameters.md`：实现 `if`、`where`、`trim`、`foreach`、`${}` 安全边界、Map/Bean/Collection 参数对象与 TypeHandler 注册。
4. `04-mybatis-cache-plugin-and-nested-mapping.md`：实现一级/二级缓存的边界、缓存键、更新清理、Interceptor 链、ResultMap、association/collection 嵌套映射。
5. `05-mybatis-types-transactions-and-ecosystem.md`：完善 TypeHandler、JdbcType、主键回填、批处理、Transaction 抽象、DataSource/连接池、Spring 事务集成边界。

每篇控制在一个清晰的可运行增量，包含目标、设计、核心代码、测试场景、与真实 MyBatis 对照和下一篇预告；不把五个主题继续挤在一篇文章里。统一使用 `categories: [Mybatis]`、系列编号文件名和文章同名资源目录。同步更新 `source/about/index.md` 的系列文章目录、`README.md` 内容地图（Mybatis 系列篇数），必要时为新 SVG 配图补资源。完成后运行 SVG/引用检查与 `pnpm build`，确认每篇文章能生成。